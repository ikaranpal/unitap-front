import Modal from 'components/common/Modal/modal';

const ChooseProviderModal = () => {
	return (
		<Modal closeModalHandler={() => {}} isOpen={false}>
			salam
		</Modal>
	);
};

export default ChooseProviderModal;
